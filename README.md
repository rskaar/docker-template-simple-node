# docker-template-simple-node

Simple node express app (chat) to test docker setup


TODO: Create doc!


## Setup

### Docker
```
docker-compose build
docker-compose up
```

Accessible through `http://*:3000`

### Yarn
Update `yarn.lock` and add local node_modules folder
```
npm run updateyarn
```
